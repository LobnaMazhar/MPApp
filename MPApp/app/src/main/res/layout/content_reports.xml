<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.materialplanning.vodafone.mpapp.reportsActivity"
    tools:showIn="@layout/activity_reports">

    <Button
        style="?android:attr/buttonStyleSmall"
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:text="Get BOM"
        android:id="@+id/getBOMButton"
        android:onClick="getBOM"
        android:layout_below="@+id/reportsListView"
        android:layout_alignRight="@+id/reportsListView"
        android:layout_alignEnd="@+id/reportsListView"
        android:layout_marginTop="20dp"
        android:background="@color/vodafoneRed"
        android:textColor="@color/white"
        android:textStyle="bold"/>

    <ListView
        android:layout_width="wrap_content"
        android:layout_height="400dp"
        android:id="@+id/reportsListView"
        android:layout_alignParentTop="true"
        android:divider="@color/colorPrimary"
        android:dividerHeight="1dp"/>
</RelativeLayout>
